# 猜硬币 · 游戏规则

## 概述

玩家拥有**初始本金 1000**，通过猜测硬币的正反面进行游戏。可向**银行借款**（每局按 **0.3% 复利** 计息，借款额度见银行系统）。**总资产**：界面显示为 本金 − 负债 + 保险箱；**破产判定**仅看 本金 − 负债（不含保险箱），**连续三轮为负即破产（游戏终止）**。游戏中仅有一种模式：**增长模式**，分为三档（20 / 50 / 100），可随时切换。另有**保险箱**可存入一笔资金，锁定期与存入下限随使用次数递增（见保险箱一节）。

---

## 增长模式（三档）

玩家在**开翻前**可选择档位：**20 档**、**50 档**、**100 档**，游戏中可随时切换。**20 档**仅当当前**现金流 &lt; 100** 时可选；现金流 ≥ 100 时不可选 20 档，须选 50 档或 100 档。

### 20 档

- **猜对**：本金 **+20**。
- **连续猜对奖励**：若上一局也猜对，本局额外 **+5**（即本局共 **+25**）。
- **猜错**：本金 **-20**。
- **连续猜错惩罚**：若上一局也猜错，本局额外 **-10**（即本局共 **-30**）。

### 50 档

- **猜对**：本金 **+50**；连续猜对额外 **+10**。
- **猜错**：本金 **-50**；连续猜错额外 **-10**。

### 100 档

- **猜对**：本金 **+100**；连续猜对额外 **+20**。
- **猜错**：本金 **-100**；连续猜错额外 **-15**。

连续以「猜对」或「猜错」计，一旦结果相反即重新开始计连续次数。开翻前须保证当前本金 ≥ 该档**最大可能亏损**（即 猜错 + 连续猜错 的扣款）。

---

## 保险箱

- **存入**：仅当保险箱**为空**（无未解锁资金）时可存**一笔**。存入金额下限随「使用次数」递增：**第一次使用** ≥ 现金流 **70%**，**提现后再存（第二次使用）** ≥ **80%**，**再提现再存（第三次及以后）** ≥ **90%**（之后一直锁定 90%）。存入后本金减少该金额，保险箱增加该金额，并记录「存入时轮次」。
- **锁定期（递增）**：**第一次使用**锁 **10 轮**；锁定期满后若选**保留**（先加 0.4% 奖励），则进入新锁定期，每次保留再 +10 轮（10→20→30…）。**提现后再次存入**：本次锁 **40 轮**，保留后 50→60…；再提现再存锁 **70 轮**，以此类推（每次提现后再存，基础锁定期 +30 轮）。
- **锁定期内**：不可提现、不可再存第二笔。每完成一局，若保险箱有锁定期内资金，则对该笔钱按 **0.35% 复利** 计息一次：`保险箱余额 *= (1 + 0.0035)`。
- **提现**：保险箱余额全部加回本金，保险箱置空，可再次存入；下次存入的锁定期与存入下限按上一条递增。
- **单笔**：同一时间只有一笔「在箱」资金；提现后才可再次存入新的一笔。
- **结算与破产**：保险箱金额不从现金流扣除（已从本金扣过）。**破产判定仍只看 总资产 = 本金 − 负债**，不包含保险箱。

---

## 银行系统

- **借款额度**：单次借款不得超过当前**现金流**（本金 − 负债，不含保险箱）的 **20%**。游戏进行满 **50 轮**后，会显示**能力指数** = 猜中次数 ÷ 当前总猜测次数（猜中率）；此时借款额度上限提高为 现金流的 **20% + 30% × 能力指数**（能力为 0 时 20%，能力为 100% 时 50%）。
- **借款**：在额度内输入金额并点击「借款」，本金与负债同时增加该金额（总资产不变）。
- **还款**：可随时输入金额并点击「还款」，从当前本金（现金）中扣除该金额用于还债，本金与负债同时减少；实际还款额不会超过当前本金与当前负债的较小值。
- **利率**：每完成一局（开翻一次）后，负债按 **0.3% 复利** 计息：新负债 = 原负债 × (1 + 0.003)。
- **总资产**：界面左上角与右上角图表中的总资产/资金 = 本金 − 负债 + 保险箱（含保险箱）；破产判定仅按 本金 − 负债（不含保险箱）；历史记录表格中的总资产列为每局后的 本金−负债（不含保险箱）。
- **破产（游戏终止）**：**长期负资产**判定——当**连续三轮**总资产均为负时游戏结束；若某一轮总资产回到正数，则连续计数清零。

---

## 游戏流程

1. 可选：在「借款」框输入金额并点击「借款」（不得超过当前借款额度），以增加本金与负债。
2. 可选：在「保险箱」区存入一笔（保险箱为空时，存入金额须 ≥ 现金流 70%/80%/90%，依此前提现次数而定）。
3. 选择增长档位（20 档 / 50 档 / 100 档）。
4. 选择「正面」或「反面」。
5. 点击「开翻 1 次」，硬币随机出现正面或反面（采用加密随机源，接近真随机），立即按当前档位规则结算并更新本金，负债按 0.3% 复利计息一次；若保险箱有锁定期内资金则按 0.35% 复利计息一次。
6. 若保险箱锁定期满，可选择「提现」或「保留」（保留加 0.4% 并进入新锁定期）。
7. 可继续借款、保险箱操作、选择档位与正/反并开翻，直至连续三轮 本金−负债 为负（破产/清算）。

---

## 清算与历史

- **清算**：某一局结算后，若**连续三轮** 本金−负债 均为负，则游戏结束（破产），所有操作禁用，页面显示「清算」。
- **历史记录**：记录所有操作及对应轮次。**开翻**：轮次为 1、2、3…（第几局），并记录档位、下注、猜测、答案、损益、本金、负债、保险箱、总资产。**借款 / 还款 / 保险箱存入 / 保险箱提现 / 保险箱保留**：轮次为「第几局后」（0 表示第 1 局前），并记录类型、金额、本金、负债、保险箱、总资产。
- **下载**：可随时导出上述历史为 CSV 文件（UTF-8，含表头，含类型列）。

---

## 界面说明

- **左上**：本金、负债、总资产（含保险箱）；下方注明**破产判定：本金−负债（不含保险箱）**。开翻按钮不可点击时会显示原因（未选正/反、本金不足、已清算）。
- **右上**：历史数据图。蓝线为「正面次数 − 反面次数」随轮次变化；黄线为**资金（含保险箱）**随轮次变化。图上标注**正/反 = 正面次数 / 反面次数**。
- **右下**：历史记录表格与「下载」按钮，可滚动查看每局详情并导出 CSV。
